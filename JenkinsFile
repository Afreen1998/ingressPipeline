node {  
    stage('Build') { 
        // 
    }
    stage('Test') { 
        // 
    }
    stage('Deploy') { 
        sh "pwd"
        sh "rm -rf ingress"
        sh "mkdir ingress"
        sh " git clone https://github.com/Afreen1998/ingressPipeline ingress"
        sh "cd $workspace/ingress"
        sh "ls"
        sh "kubectl get pods"
        sh "kubectl apply -f ingressResource"
    }
}
